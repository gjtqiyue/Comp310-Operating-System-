Use linux machine to execute

Execute the makefile to compile the program
run ./mysh
create testfile.txt in order to run command in the file
then run ./mysh < testfile.txt

NOTE: if the program doesn't recognize the command and run into
infinite loop, make sure the encoding and the sequence coding of the
txt file is UTF-8 and LF
